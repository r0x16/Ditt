.base:
  tags:
    - shell
  variables:
    TF_HTTP_ADDRESS: "$STATE_URL/$CI_ENVIRONMENT_SLUG"
    TF_HTTP_LOCK_ADDRESS: "$STATE_URL/$CI_ENVIRONMENT_SLUG/lock"
    TF_HTTP_UNLOCK_ADDRESS: "$STATE_URL/$CI_ENVIRONMENT_SLUG/lock"
    INVENTORY_FILE: '$CI_PROJECT_DIR/inventory_$CI_ENVIRONMENT_NAME.txt'
    OUTPUTS_FILE: '$CI_PROJECT_DIR/outputs_$CI_ENVIRONMENT_NAME.json'